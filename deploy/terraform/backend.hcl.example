# Copy to backend.hcl and pass: terraform init -backend-config=backend.hcl
# Only after running deploy/scripts/bootstrap-state.sh. Default apply uses local state.

# bucket         = "gnss-ew-tfstate-CHANGE-ME"
# key            = "dev/terraform.tfstate"
# region         = "us-east-1"
# encrypt        = true
# dynamodb_table = "gnss-ew-tfstate-lock"
